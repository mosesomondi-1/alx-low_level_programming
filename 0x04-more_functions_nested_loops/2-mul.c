#include "main.h"
/**
 * mul -  multiplies two integers
 * @a: The first number to multiply to
 * @b: The second number to multiply to the first number
 * Return: The result of the multiplication between aand b
 */
int mul(int a, int b)
{
	return (a * b);
}
